apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deployment-api.fullname" . }}-test-connection"
  labels:
    {{- include "deployment-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deployment-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
